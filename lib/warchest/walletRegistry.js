'use strict';

/**
 * Legacy entrypoint for the BootyBox-backed wallet registry.
 * Prefer requiring `lib/wallets/walletRegistry.js` directly.
 */
module.exports = require('../wallets/walletRegistry');
