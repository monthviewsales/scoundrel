'use strict';

/**
 * This command entrypoint now lives in lib/cli/warchestCli.js.
 * Keeping this shim for backwards compatibility with older requires.
 */
module.exports = require('../lib/cli/warchestCli');
